# Vercel Migration Notes

- Vercel must not be switched during skeleton setup.
- Future deployment migration must set Vercel root directory to `apps/web` only after the migrated app validates.
- Environment variables must be copied manually and never committed.
