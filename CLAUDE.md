# Baja411 Claude Code Instructions

Read `REPO_MAP.md` first.

The repository root is now the Next.js app root.

Patch the smallest safe part and avoid broad rewrites.

Do not casually change map behavior, Drive Mode, Plan Mode, GPS/heading/recenter/snap-back, weather, SOS, auth, Prisma, Vercel config, or environment variables.

Validation commands:

```bash
npm run lint
npm run build
```
