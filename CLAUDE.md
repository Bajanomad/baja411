# Baja411 Claude Code Instructions

Start at repository root. Do not assume a nested `baja411/` app.

Read `REPO_MAP.md` first, then exact files.

Validate from root:
- `npm run lint`
- `npm run build`
