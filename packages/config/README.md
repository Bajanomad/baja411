# @bajasur411/config (Placeholder)

This package is reserved for future shared TypeScript, lint, formatting, and styling config.

Do not centralize config until the web app validates after migration.
