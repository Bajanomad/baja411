# @bajasur411/database (Placeholder)

Prisma remains inside `apps/web/prisma` during initial migration.

This package is reserved for future shared database tooling only.

Do not move Prisma here yet.
