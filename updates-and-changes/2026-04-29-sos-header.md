# 2026-04-29 - Move SOS button into header

- Moved SOS button from global floating placement in layout into the main nav header.
- Kept SOS text, emergency route, and aria label unchanged.
- Kept hamburger menu behavior and avoided map/weather/emergency content changes.
- Added `id="top"` on landing hero section to support `/#top` header logo link.
